# Intel Super Builder gRPC Connection Settings
# 
# Copy this file to .env and update the values for your environment.
# 
# For WSL users: Set SUPERBUILDER_GRPC_HOST to your Windows host IP
# Find it by running in WSL: ip route show default | awk '{print $3}'
# Or from Windows PowerShell: Get-NetIPAddress | Where-Object {$_.InterfaceAlias -like "*WSL*"}
#
# For Windows native: Use "localhost"

# gRPC Host - Windows host IP for WSL, or "localhost" for native Windows
SUPERBUILDER_GRPC_HOST=localhost

# gRPC Port - Default Intel Super Builder port
SUPERBUILDER_GRPC_PORT=5006
